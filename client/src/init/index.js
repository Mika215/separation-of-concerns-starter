// variables
(function(){
    const activityList = document.getElementById('ordered-list');
    const activityInput = document.getElementById('todo-input');
    const addButton = document.getElementById('todo-button');
    // functions
    const addActivity = function(){


    }

    // events
    addButton.addEventListener('click', addActivity);
}());

